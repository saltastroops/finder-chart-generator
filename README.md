# finder-chart-generator

Website for generating SALT finder charts

## Deployment

You can deploy the Finder Chart Generator via Docker:

```bash
docker compose up --build -d
```

The deployed Finder Chart Generator is listening on port 6789.
